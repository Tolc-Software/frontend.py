include(CMakeFindDependencyMacro)
find_dependency(IR)

include("${CMAKE_CURRENT_LIST_DIR}/ConanHelpers.cmake")
set(packages @FRONTEND.PY_CONAN_REQUIRES_PACKAGES@)
find_conan_packages(REQUIRES ${packages})

include("${CMAKE_CURRENT_LIST_DIR}/Frontend.pyTargets.cmake")
